<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TecnoGlass | Contato</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            -webkit-text-size-adjust: 100%;
            -ms-text-size-adjust: 100%;
        }

        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .header {
            text-align: center;
            padding: 20px 0;
            background-color: #002a5b;
            color: white;
            border-radius: 8px 8px 0 0;
        }

        .header img {
            width: 150px;
        }

        .content {
            padding: 20px;
            color: #333333;
        }

        .content h1 {
            font-size: 24px;
            margin-bottom: 20px;
            color: #002a5b;
        }

        .content p {
            font-size: 16px;
            line-height: 1.5;
            margin-bottom: 20px;
        }

        .footer {
            text-align: center;
            padding: 20px;
            background-color: #f8f8f8;
            color: #777777;
            border-radius: 0 0 8px 8px;
            font-size: 14px;
        }

        .footer a {
            color: #002a5b;
            text-decoration: none;
        }
    </style>
</head>

<body>
    <div class="email-container">
        <div class="header">
            <img src="https://tecnoglassbrasil.com.br/assets/img/logoWhite.png" alt="TecnoGlass Logo">
        </div>
        <div class="content">
            <h1>{{subject}}</h1>
            <p>Olá, {{name}}, {{email}}</p>
            <p>{{message}}</p>
        </div>
        <div class="footer">
            <p>&copy; 2024 TecnoGlass. Todos os direitos reservados.</p>
            <p>Mensagem de: <a href="mailto:{{email}}">{{email}}</a></p>
        </div>
    </div>
</body>

</html>